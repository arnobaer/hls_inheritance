#ifndef config_h
#define config_h

#include <cstddef>

namespace config {

// Inputs

static const size_t EG_COUNT = 12;
static const size_t TAU_COUNT = 12;
static const size_t JET_COUNT = 12;
static const size_t MUON_COUNT = 8;

// Outputs

static const size_t ALGO_COUNT = 512;

} // namespace config

#endif
